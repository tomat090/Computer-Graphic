#include "vec3dimention.h"

/*template<typename T>
Vec3Dimention<T> Vec3Dimention<T>::vect(const Vec3Dimention<T> &v) const
{ 
	float xx = y * v.z - z * v.y;
	float yy = z * v.x - z * v.z;
	float zz = x * v.y - y * v.x;
	return Vec3Dimention<T>(xx, yy, zz);
}*/